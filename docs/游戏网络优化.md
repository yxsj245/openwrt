# 游戏场景网络优化指南

> **适用分支**：J6412 + i226-V 硬件定制分支  
> **目标**：针对 Intel Elkhart Lake J6412 四核 + 4×Intel i226-V 2.5G 网卡硬件组合，提供最大化的低延迟网络优化  
> **原则**：新增软件包均为 OpenWrt 官方仓库提供的成熟工具，不引入激进内核补丁或第三方闭源模块

> **2026-08-17 更新**：固件已用 QoSmate 同时取代 SQM 和原客户端限速插件。本文中 SQM 页面的旧操作截面仅作 CAKE 参数原理参考，实际配置请使用 **网络 → QoSmate**，并参见 [QoSmate 使用与迁移说明](qosmate.md)。

---

## 一、当前已有优化（已编译进固件）

以下优化项已在当前 `config-temp` 中启用，编译后自动生效：

### 1.1 QoSmate 智能队列与客户端限速

| 软件包 | 状态 | 作用 |
|--------|------|------|
| `qosmate` | ✅ 已启用 | CAKE/HFSC、DSCP 分类和客户端限速 |
| `luci-app-qosmate` | ✅ 已启用 | LuCI Web 管理界面 |
| `kmod-sched-cake` | ✅ 已启用 | CAKE 队列调度算法内核模块 |
| `kmod-sched-core` | ✅ 已启用 | tc 流量控制核心模块 |
| `kmod-ifb` | ✅ 已启用 | 中间功能块设备，SQM 入向整形依赖 |

**效果**：QoSmate 统一管理 CAKE 队列、DSCP 优先级和每客户端带宽上限，既抑制 bufferbloat（缓冲膨胀），也取代了原有的独立客户端限速插件。

### 1.2 nftables 软件流量分载

| 软件包 | 状态 | 作用 |
|--------|------|------|
| `kmod-nft-offload` | ✅ 已启用 | nftables 流量分载引擎 |

**效果**：将已建立连接的路由/NAT 转发从 CPU 逐包处理转移到快速路径，降低 CPU 使用率，减少转发延迟。与 firewall4（nftables）原生集成，无需额外配置。

> ⚠️ **与 QoSmate 的关系**：流量分载会绕过 tc 队列，因此不能与 QoSmate 的流量整形同时生效。游戏场景建议优先使用 QoSmate，并关闭 firewall4 流量分载。

### 1.3 全锥型 NAT（NAT1 / Fullcone NAT）

| 软件包 | 状态 | 作用 |
|--------|------|------|
| `kmod-nft-fullcone` | ✅ 已启用 | 全锥型 NAT 内核模块 |
| `luci-app-fullconenat` | ✅ 已启用 | LuCI Web 管理界面 |

**效果**：将 NAT 类型从"对称型"（NAT4）提升为"全锥型"（NAT1），改善 P2P 联机匹配成功率和游戏主机的 NAT 类型检测。对 Switch/PS5/Xbox 的在线联机体验有明显帮助。

### 1.4 TCP BBR 拥塞控制

| 软件包 | 状态 | 作用 |
|--------|------|------|
| `kmod-tcp-bbr` | ✅ 已启用 | BBR 拥塞控制算法内核模块 |

**效果**：BBR 相比传统 CUBIC 算法在高延迟、有丢包的网络环境中显著提升吞吐量，减少 TCP 重传引起的延迟抖动。启用后需将系统默认拥塞控制算法切换为 `bbr`（见第五章 sysctl 配置）。

### 1.5 DNS 服务

| 软件包 | 状态 | 作用 |
|--------|------|------|
| `dnsmasq-full` | ✅ 已启用 | 增强版 DNS/DHCP 服务 |
| `smartdns` | ✅ 已启用 | 并发查询上游 DNS 并优选结果 |
| `luci-app-smartdns` | ✅ 已启用 | SmartDNS LuCI 管理界面 |

**效果**：dnsmasq-full 继续监听 53 端口并提供 DHCP、本地域名和 nftset，DNS 请求转发到监听 6053 端口的 SmartDNS，由 SmartDNS 负责上游优选。详细配置参见 [SmartDNS 使用说明](smartdns.md)。

### 1.6 内核级优化

| 配置项 | 状态 | 作用 |
|--------|------|------|
| `CONFIG_TARGET_OPTIMIZATION` | ✅ `-Os -pipe` | 保持上游兼容性与固件体积优化 |
| `CONFIG_KERNEL_CGROUP_SCHED` | ✅ | CPU CGroup 调度 |
| `CONFIG_KERNEL_MPTCP` | ✅ | 多路径 TCP 支持 |
| `CONFIG_KERNEL_NF_CONNTRACK_TIMEOUT` | ✅ | 连接跟踪超时策略支持 |

### 1.7 硬件定制优化（本分支专属）

| 软件包 | 状态 | 作用 |
|--------|------|------|
| `nic-tuning-gaming` | ✅ 已启用 | i226-V 网卡自动调优（队列/中断合并/Ring Buffer/Offload） |
| `miniupnpd-nftables` | ✅ 已启用 | UPnP 端口映射服务（nftables 版本） |
| `luci-app-upnp` | ✅ 已启用 | UPnP LuCI Web 管理界面 |

**nic-tuning-gaming 包说明**：含两个一次性启动脚本：
- `nic-tuning`：检测 i226-V 网卡（PCI ID 8086:125c），按 UCI 配置应用均衡、低延迟或吞吐模式
- `irq-affinity`：仅在显式选择手工 IRQ 模式且 `irqbalance` 已停止时绑定 IRQ，默认不参与分配

默认使用 `balanced` 模式和 `irqbalance`。实体机长期运行数据表明四个硬件队列均有实际流量，因此保留每端口最多四队列；Ring Buffer 提高到 1024，并恢复 TSO/GSO/GRO，由 CAKE 的 `split-gso` 处理大包拆分。

---

## 二、基础优化工具（已编译进固件）

以下两项为兼容性极高的成熟工具，已在本分支中编译进固件。

### 2.1 irqbalance（中断负载均衡）

| 属性 | 值 |
|------|-----|
| 软件包名 | `irqbalance` |
| LuCI 界面 | `luci-app-irqbalance`（可选） |
| 资源占用 | 很小（守护进程，< 5MB 内存） |
| 兼容性 | 所有多核 CPU，无已知副作用 |
| 官方仓库 | OpenWrt 官方自带 |

**作用**：在多核 CPU 上自动将网卡等硬件的中断请求（IRQ）均匀分配到各核心，避免所有中断集中在 CPU0 导致单核瓶颈。

**为什么本分支需要**：x86 软路由通常有多个 CPU 核心。本硬件 J6412 为四核，搭配 4×i226-V 会产生 16 个 TxRx MSI-X 中断向量。默认由 `irqbalance` 根据实时负载分配，避免静态规则与动态守护进程互相覆盖。

**启用方式**：安装后默认自动启动，无需手动配置。

### 2.2 ethtool（网卡诊断与调优工具）

| 属性 | 值 |
|------|-----|
| 软件包名 | `ethtool`（基础版）或 `ethtool-full`（完整版） |
| LuCI 界面 | 无（命令行工具） |
| 资源占用 | 极小（命令行工具，不驻留） |
| 兼容性 | 所有以太网卡 |
| 官方仓库 | OpenWrt 官方自带 |

**作用**：
- 查看网卡驱动版本、链路状态、速率协商信息
- 查看/调整网卡 Ring Buffer 大小
- 查看/调整中断合并（coalescing）参数
- 查看/调整硬件 offload 特性（TSO/GSO/GRO）
- 网卡硬件自检和统计信息

**为什么本分支需要**：
- `ethtool` 是 Linux 网络调试的标配工具，也是 `nic-tuning-gaming` 包的依赖
- 为 i226-V 网卡的深度调优提供基础工具
- 排查网卡问题（掉线、协商速率异常）时必需

**建议安装 `ethtool` 基础版**，`ethtool-full` 增加了网卡固件刷写等高级功能，本分支不需要。

### 2.3 UPnP 端口映射（miniupnpd）

| 属性 | 值 |
|------|-----|
| 软件包名 | `miniupnpd-nftables` |
| LuCI 界面 | `luci-app-upnp` |
| 资源占用 | 极小（守护进程，< 10MB 内存） |
| 兼容性 | 需要 nftables（firewall4） |
| 官方仓库 | OpenWrt packages feed |

**作用**：允许局域网内的游戏主机（Switch/PS5/Xbox/PC）自动请求路由器开放端口，改善 P2P 联机和语音聊天的 NAT 穿透成功率。

**为什么本分支需要**：i226-V + J6412 为 4 口 2.5G 软路由，典型场景为家庭/小型工作室网关，多台游戏主机需要 UPnP 端口映射。

**安全提醒**：UPnP 允许任意内网设备开放端口，存在安全风险。建议仅在信任的内网环境启用，并通过 LuCI 界面限制允许使用 UPnP 的设备范围。

---

## 三、i226-V 网卡硬件调优详情

以下调优参数由 `nic-tuning-gaming` 包的 `nic-tuning` 脚本在启动时自动应用。脚本根据 sysfs PCI ID 识别 Intel i226-V，不依赖固定接口名称。

### 3.1 ethtool 调优参数

| 模式 | 队列 | Ring Buffer | 中断合并 | TSO/GSO/GRO | 用途 |
|------|------|-------------|----------|-------------|------|
| `balanced` | 自动取在线 CPU 数和网卡上限的较小值 | 1024 | 保留驱动默认值 | 开启 | 默认；兼顾 CAKE 延迟、2.5G 吞吐和突发流量 |
| `low-latency` | 同上 | 512 | 关闭自适应，目标 8 μs | 关闭 | 经过延迟和丢包对比测试后使用 |
| `throughput` | 同上 | 4096 | 启用自适应 | 开启 | 大文件传输或内网吞吐优先 |

配置文件为 `/etc/config/nic-tuning`。切换模式后执行 `/etc/init.d/nic-tuning apply`，失败项会写入中文系统日志，不再静默忽略。

### 3.2 IRQ 亲和性策略

默认 `irq_mode` 为 `irqbalance`，手工脚本直接退出。只有将其改为 `manual` 并停止 `irqbalance` 后，脚本才会通过 `smp_affinity_list` 将全部 i226-V TxRx IRQ 轮询绑定到 CPU1-CPU3。若检测到 `irqbalance` 仍在运行，脚本会拒绝修改并记录错误，防止两个策略相互覆盖。

### 3.3 控制台菜单识别显示

`/usr/lib/console-menu/display.sh` 中增加了 i226-V 检测逻辑：物理终端登录时，如果检测到 i226-V 网卡，菜单会在版本信息下方显示：

```
  硬件: 已检测到 Intel i226-V 2.5G
  网卡调优: 均衡模式
```

仅在 PCI ID 完全匹配 `8086:125c` 时显示。

### 3.4 PCIe ASPM 建议

```bash
# 检查当前 ASPM 策略
cat /sys/module/pcie_aspm/parameters/policy

# 建议设为 performance（省电模式会让 PCIe 链路空闲降速，重新协商有延迟）
echo "performance" > /sys/module/pcie_aspm/parameters/policy
```

> ⚠️ ASPM 调优影响功耗和发热，J6412 是被动散热，如设 `performance` 请留意温度。

---

## 四、CAKE 游戏场景参数参考（请在 QoSmate 配置）

以下参数来自原 SQM 页面，保留用于说明 CAKE 配置原理。当前固件请在 LuCI → 网络 → QoSmate 中配置对应参数，具体操作参见 [QoSmate 使用与迁移说明](qosmate.md)。

### 4.1 核心参数

```
┌─ Basic Settings ─────────────────────────────┐
│ Interface:       wan (或 pppoe-wan)           │
│ Download:        实际带宽的 85%-90%            │
│ Upload:          实际带宽的 85%-90%            │
└──────────────────────────────────────────────┘

┌─ Queue Discipline ───────────────────────────┐
│ Queueing Discipline:  cake                    │
│ Queue Setup Script:   layer_cake.qos          │
│ Advanced:             ✅ 勾选                  │
│  ┌─ Advanced Options ────────────────────┐    │
│  │ Ingress (download):                   │    │
│  │   nat ingress diffserv8 dual-dsthost  │    │
│  │ Egress (upload):                      │    │
│  │   nat ack-filter diffserv8 dual-srchost│   │
│  └───────────────────────────────────────┘    │
└──────────────────────────────────────────────┘

┌─ Link Layer Adaptation ──────────────────────┐
│ Link Layer:         根据链路类型选择            │
│  - 以太网/光纤:     ethernet, overhead 44      │
│  - PPPoE (VDSL):    ethernet, overhead 26/34  │
│  - DOCSIS (Cable):  ethernet, overhead 22     │
└──────────────────────────────────────────────┘
```

### 4.2 参数说明

| 参数 | 位置 | 作用 |
|------|------|------|
| `nat` | iqdisc/eqdisc | 利用内核 NAT 信息区分内网主机，实现**每主机公平**而非仅每流公平 |
| `ingress` | iqdisc | 入向整形模式，让下载方向带宽控制更贴近真实瓶颈 |
| `ack-filter` | eqdisc | 过滤冗余 TCP ACK 包，节省上传带宽（非对称宽带优势明显） |
| `diffserv8` | iqdisc/eqdisc | 8 级 DSCP 差分服务，相比默认 `diffserv4` 更细粒度优先级 |
| `dual-srchost` | eqdisc | 上传方向：同时按源主机和流做双维度隔离 |
| `dual-dsthost` | iqdisc | 下载方向：同时按目标主机和流做双维度隔离 |
| `layer_cake.qos` | script | Layer Cake 模式，基于 DSCP 做优先级区分 |

### 4.3 带宽限制建议

带宽限制值应设为**实际测速结果的 85%-90%**，让路由器的 SQM 队列成为瓶颈控制点，而非上游 ISP 设备的不可控大缓存。

```
示例：
  实际测速：500Mbps ↓ / 50Mbps ↑
  建议设置：450Mbps ↓ / 42Mbps ↑
```

---

## 五、sysctl 内核参数推荐配置

以下配置为通用兼容性参数，直接写入 `/etc/sysctl.d/10-gaming.conf` 即可生效。不会降低非游戏场景的吞吐量。

```ini
# ============================================================
# 游戏场景通用内核优化（兼容性优先）
# 适用: OpenWrt 通用分支，不依赖特定硬件
# ============================================================

# --- TCP 拥塞控制 ---
# 启用 BBR（需 kmod-tcp-bbr 已加载）
net.ipv4.tcp_congestion_control = bbr
# 默认队列算法（配合 CAKE 使用）
net.core.default_qdisc = fq_codel

# --- TCP 低延迟选项 ---
# 空闲后不进入慢启动（避免游戏间隔后突发降速）
net.ipv4.tcp_slow_start_after_idle = 0
# 启用 TCP Fast Open（减少连接建立 RTT）
net.ipv4.tcp_fastopen = 3

# --- UDP 缓冲区（游戏以 UDP 小包为主） ---
# 默认接收缓冲区 128KB（小缓冲 = 低排队延迟）
net.core.rmem_default = 131072
# 默认发送缓冲区 128KB
net.core.wmem_default = 131072

# --- 连接跟踪优化 ---
# 缩短 UDP 超时（游戏心跳包通常 < 30s 间隔）
net.netfilter.nf_conntrack_udp_timeout = 30
net.netfilter.nf_conntrack_udp_timeout_stream = 60

# --- 2.5G 高速链路优化 ---
# 增大入向包队列（2.5G 突发更猛，i226-V 多队列并发）
net.core.netdev_max_backlog = 16384
# 允许软中断处理更多包再让出 CPU
net.core.netdev_budget = 600
```

> ⚠️ 以下参数**不加入通用分支**，因为它们为吞吐量优先或激进配置，可能影响非游戏场景。硬件特定分支可按需启用：
> - `net.core.rmem_max` / `wmem_max`（大缓冲 = 高吞吐，与游戏低延迟理念冲突）
> - `net.ipv4.tcp_tw_reuse = 1`（快速回收 TIME_WAIT，有极低概率的端口复用冲突）
> - `net.ipv4.tcp_notsent_lowat`（强制低水位推送，增加 CPU 开销）

---

## 六、推荐实施步骤

### Step 1：修改 config-temp，新增软件包

在 `config-temp` 中启用以下项（本分支已全部启用）：

```diff
+CONFIG_PACKAGE_ethtool=y
+CONFIG_PACKAGE_irqbalance=y
+CONFIG_PACKAGE_luci-app-irqbalance=y
+CONFIG_PACKAGE_nic-tuning-gaming=y
+CONFIG_PACKAGE_miniupnpd-nftables=y
+CONFIG_PACKAGE_luci-app-upnp=y
```

### Step 2：编译并升级固件

```
make defconfig && make -j$(nproc)
```

### Step 3：首次启动后手动配置

1. **QoSmate**：LuCI → 网络 → QoSmate → 核对 WAN 设备、上下行带宽及第四章所述 CAKE 参数
2. **sysctl**：将第五章配置写入 `/etc/sysctl.d/10-gaming.conf`，执行 `sysctl -p /etc/sysctl.d/10-gaming.conf`
3. **Fullcone NAT**：LuCI → Network → Fullcone NAT → 按需开启各区域
4. **BBR**：确认 `sysctl net.ipv4.tcp_congestion_control` 输出为 `bbr`
5. **UPnP**：LuCI → Services → UPnP → 配置允许的设备范围（建议白名单模式）
6. **NIC Tuning**：默认均衡模式自动运行，执行 `uci get nic-tuning.settings.profile` 和 `ethtool -g eth0` 核对配置
7. **IRQ 分配**：默认确认 `irqbalance` 正在运行；只有显式使用手工模式时才检查 CPU1-CPU3 绑定
8. **CPU 微码**：J6412 应显示微码 `0x1a`，且启动日志应包含早期微码加载记录

---

## 七、硬件特定优化状态

以下优化项**已在本分支部分实现**或**预留供后续参考**：

| 优化项 | 本分支状态 | 说明 |
|--------|-----------|------|
| NIC 分档调优 | ✅ 已实现 | 默认均衡模式恢复硬件分载，低延迟模式可按需关闭 |
| IRQ 分配 | ✅ 已实现 | 默认使用 irqbalance，手工模式带冲突保护 |
| UPnP（miniupnpd） | ✅ 已实现 | 已编译进固件 |
| CPU 性能模式 (`performance` governor) | ❌ 未启用 | J6412 被动散热，锁高频有风险 |
| SmartDNS 并发查询 | ✅ 已实现 | 已采纳通用分支实现，由 dnsmasq-full 转发请求 |
| VPN 协议优化（WireGuard MTU 等） | ❌ 预留 | 通用分支不预装 VPN |
| WiFi 高级调优（AQL、airtime fairness） | ❌ 预留 | 本硬件无 Wi-Fi 芯片 |

---

## 八、参考资料

- [OpenWrt SQM (Smart Queue Management) 官方文档](https://openwrt.org/docs/guide-user/network/traffic-shaping/sqm)
- [QoSmate 上游项目](https://github.com/hudra0/qosmate)
- [OpenWrt CAKE 调优社区讨论](https://forum.openwrt.org/t/fine-tune-your-cake-setup/216242)
- [Linux 内核网络参数文档](https://www.kernel.org/doc/Documentation/networking/ip-sysctl.txt)
- [BBR 拥塞控制算法](https://github.com/google/bbr)
- [bufferbloat 项目](https://www.bufferbloat.net/)

---

> **文档版本**: v2.1
> **适用分支**: J6412 + i226-V 硬件定制 (hardware-custom)  
> **最后更新**: 2026-08-17
